{
  "name": "fx-trading",
  "version": "0.1.0",
  "private": true,
  "homepage": "http://hindo.github.io/fx-trading",
  "dependencies": {
    "react": "^16.4.2",
    "react-dom": "^16.4.2",
    "react-redux": "^5.0.7",
    "react-router-dom": "^4.3.1",
    "react-scripts": "1.1.4",
    "redux": "^4.0.0",
    "redux-thunk": "^2.3.0"
  },
  "scripts": {
    "react:start": "react-scripts start",
    "react:build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "sass:watch": "npx sass --watch src:src",
    "sass:build": "npx sass --update src:src",
    "start": "nf start",
    "build": "yarn run sass:build && yarn run react:build"
  },
  "devDependencies": {
    "foreman": "^3.0.1",
    "node-sass": "^7.0.0"
  }
}
